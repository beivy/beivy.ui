import type { BeTLayout } from '../core/type'

export const twClassName = (() => {
    return (propName: string, propAttribute: string) => {}
})()
export const layout = ({
    display,
    position,
    inset,
    boxSizing,
    float,
    clear,
    isolation,
    objectFit,
    objectPosition,
    overflow,
    overscroll,
    visibility,
    zIndex,
}: BeTLayout) => {}
